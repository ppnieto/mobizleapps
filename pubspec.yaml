name: mobizleapps
description: "A new Flutter package project."
version: 0.0.1
homepage:

environment:
  sdk: '>=3.2.6 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_dashboard:
    path: "../firebase_dashboard_2"


  firebase_core:
  cloud_firestore:
  firebase_auth:


  get:
  quickalert:
  package_info_plus: 

  event_hub: 
  uuid:


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


# The following section is specific to Flutter packages.
flutter:

